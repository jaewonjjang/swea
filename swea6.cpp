#include <iostream>
#include <vector>

using namespace std;

#define MAX 16

typedef struct {
    int t, p;
}meeting;

int N;
meeting marray[MAX];
bool check[MAX];
vector
int total_pay;
int result;
void input(){
    cin >> N;

    for(int i=1;i<=N;i++){
        cin >> marray[i].t >> marray[i].p;
    }

    total_pay=0;
    result = 0;
}

void solve(){

}

void make_meeting(int idx){
    if(marray[idx].t + marray[idx].p > N) {
        for(int i=0;i<meet.size();i++){
            result +=
        }
        return;
    }
    else{

    }
}



